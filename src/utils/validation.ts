export const validateUsername = (username: string) => {
  // TODO: username validity function
  return username !== '';
};

export const validateRoomName = (roomName: string) => {
  // TODO: roomName validity function
  return roomName !== '';
};
