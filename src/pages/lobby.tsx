import { Button, Stack } from '@mui/material';

const Lobby = () => (
  <Stack sx={{ gap: 4 }}>
    <Button variant="outlined" onClick={() => {}}>
      Join
    </Button>
    <Button variant="outlined" onClick={() => {}}>
      Host
    </Button>
  </Stack>
);

export default Lobby;
